pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        // Add other repositories if needed
    }
}

rootProject.name = 'gradle-tutorial'

include 'library'
include 'application'
